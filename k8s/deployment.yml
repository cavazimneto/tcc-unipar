apiVersion: apps/v1
kind: Deployment
metadata:
  name: meudeployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: digital-ocean-deploy 
  template:
    metadata:
      labels:  
        app: digital-ocean-deploy   
    spec:
      containers:
      - name: unitcc
        image: <IMAGE>
        ports:
          - containerPort: 80

---   

apiVersion: v1
kind: Service 
metadata: 
  name: web
spec:
  selector:   
    app: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30000
  type: LoadBalancer